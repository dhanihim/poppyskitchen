<%= form_with(model: schedule) do |form| %>
  <% if schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(schedule.errors.count, "error") %> prohibited this schedule from being saved:</h2>

      <ul>
        <% schedule.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <br>
  <div class="form-group">
    <%= form.label :"Preoder Open Date" %>
    <br>
    <%= form.date_field :dateopen, class:'form-control', required:'true' %>
  </div>

  <div class="form-group">
    <%= form.label :"Preoder Close Date" %>
    <br>
    <%= form.date_field :dateclose, class:'form-control', required:'true' %>
  </div>

  <div class="form-group">
    <%= form.label :"Delivery Date"%>
    <br>
    <%= form.date_field :datedelivery, class:'form-control', required:'true' %>
  </div>


  <div class="actions">
    <%= form.submit class:'btn btn-primary' %>
  </div>
<% end %>
