<%= form_with(model: schedule) do |form| %>
  <% if schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(schedule.errors.count, "error") %> prohibited this schedule from being saved:</h2>

      <ul>
        <% schedule.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <br>
  <div class="form-group">
    <%= form.label :dateopen %>
    <br>
    <%= form.date_select :dateopen, {order: [:day, :month, :year]}, {class:'form-control', style:"display:inline-block; width:30%; margin:auto;"} %>
  </div>

  <div class="form-group">
    <%= form.label :dateclose %>
    <br>
    <%= form.date_select :dateclose, {order: [:day, :month, :year]}, {class:'form-control', style:"display:inline-block; width:30%; margin:auto;"} %>
  </div>

  <div class="actions">
    <%= form.submit class:'btn btn-primary' %>
  </div>
<% end %>
